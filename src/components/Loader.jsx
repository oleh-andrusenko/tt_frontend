import React from 'react';

const Loader = () => {
    return (
        <div className={'w-full h-[300px] flex items-center justify-center loader text-3xl font-semibold animate-pulse py-10 px-12 rounded-xl border-2 mt-10'}>

        </div>
    );
};

export default Loader;